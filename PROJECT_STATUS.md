# 项目状态

更新时间：2026-09-07

## 家具与制作

- 新增木床、木桌、木椅、木箱、落地灯、编织地毯六类程序化 3D 家具，以及纤维、配方、成品库存。采树增加 24 木材与 6 纤维，采石仍为 18 石料；鼠标、键盘劳动和自动采集共用同一资源结算。
- 家具栏显示材料持有/需求、缺口与成品数量；材料不足、暂停、失守或库存满时不可制作。制作原子扣料，摆放只扣成品，避免重复收费。
- 家具需要木地板，可和屋顶共存；一格最多一件实体家具，地毯可以铺在家具下。实体家具阻挡人物寻路，门内唯一入口格禁止放置；收起家具前须修复损伤，拆地板前须先收家具。
- 新增室内视图：家具栏自动切到较高角度的近景并隐藏屋顶，单独图标可切回；只改变显示，不改变遮雨、建筑耐久或天气。手机装修时精简状态栏，透明面板间隙和提示消息不再拦截场景点击。
- 家具沿用 0–359° 放置/已建旋转，材料、库存、家具与方向随 v1 存档保存。旧存档缺少新字段时填充完整的零库存，并验证异常库存、纤维和制作计数。
- 室内空闲时，附近床额外恢复 2 舒适度/秒，椅子额外恢复 1/秒，取最高效果。灯罩暖色自发光；木箱为陈设，不含独立储物界面。代理可收集素材，不会自动替玩家选择家具或装修。
- 18 项单元测试、类型检查与生产构建通过。新增 `scripts/check-furniture.mjs`，在 1440×960、390×844、360×740 实测制作按钮、库存扣除、床/椅/地毯鼠标摆放、门口禁放、实体旋转、收回再摆、受损家具须维修、地板拆除保护、床的舒适加成、暂停保护和新旧存档恢复。
- 三种尺寸各六件家具均通过逐模型隐藏对比的可见像素检查，非空画布、控件边界及无横向溢出通过，浏览器零错误。证据：`artifacts/furniture-qa.json`、`furniture-*.png`。图像查看工具不可用，未声称人工截图审美验收。
- 仍仅更新本地预览，未重置用户浏览器存档，未部署线上站点。

## 建筑旋转

- 2026-09-07 根据浏览器反馈将旋转交互改为同类格子建造游戏常用的四向点击控制：左右按钮或点击角度每次固定旋转 90°，角度控件滚轮可双向切换，场景内 Shift+滚轮旋转待建部件，旋转模式下点击已建部件右转、Shift+点击左转。方向只在 0°、90°、180°、270° 循环，普通滚轮缩放和右键镜头旋转保持不变。旧存档中的任意角度在读取时自动吸附到最近方向，存档版本保持 v1。
- 2026-09-07 建筑与家具改为单次待放置交互：初始及普通建造模式不再持续显示鼠标跟随预览，只有点击目录物品后才激活；成功放置一次后自动解除，再次点击场景不会重复建造。切换其他工具同样解除待放置状态。
- 2026-09-07 普通建造状态且没有待放置物品时，点击已部署的建筑或家具直接右转 90°，点击空地无操作；已激活目录物品时仍优先执行放置，避免破坏屋顶和家具叠放流程。
- 完整旋转补充：角度不再量化到 90°，新建与已建部件均支持 0–359° 连续方向、1° 滑条、数字输入和加 180° 的背面按钮。已建部件的选择与鼠标悬停分离，移到面板调角度时保留目标和选框。
- 三种尺寸已实测木墙 225°（实际正面向量 X/Z 均约 -0.707）、栅栏 135° 斜向放置、已建栅栏调整到 270°/359°、拖动范围端点及任意角度读档恢复；没有材料或耐久副作用，控件不越界，浏览器零错误。13 项单元测试、类型检查和生产构建通过。
- 修正旋转反馈不清楚的问题：此前 R 仅改变待放置部件，鼠标移至按钮时预览隐藏，切换部件后初始预览未立即带上旋转。现在提供横向/纵向按钮及角度显示，旋转后在最近指向的格位显示预览，切换部件保留方向。
- 新增 Q 旋转已建部件模式：悬停显示选框，点击转 90°；材料、耐久和部件数量保持不变。暂停、失守以及人物占位时禁止旋转实体部件。
- R 使用物理键码兼容中文输入法；原有输入框与系统组合键保护保留。
- 修复预制木屋屋顶渲染强制忽略旋转的分支，已保存的屋顶方向现在与实体一致。旧 v1 存档结构不变。
- `scripts/check-rotation.mjs` 在 1440×960、390×844、360×740 实测方向按钮、跨部件预览、90° 侧墙落地（实际几何沿 Z 长度 2、沿 X 约 0.495）、点击已建墙旋转、中文输入法键码、360° 回绕、按钮旋转、预制屋顶旋转及刷新恢复。所有控件在视口内、无横向溢出、画布非空、零浏览器错误。
- 新增旋转规则及存档测试，单元测试合计 13 项通过，类型检查通过；截图与行为证据见 `artifacts/rotation-qa.json` 和本地 `rotation-*.png`。
- 仅修改本地预览，不重置用户存档，不更新线上站点。

## 快捷键与自动生存

- 2026-09-06 本地新增 WASD / 方向键视角相对移动、碰撞阻挡与跟随镜头；E 就近劳动，F 近身攻击，T 托管，H 回屋，B/V/C/M 切换模式。松键、切出窗口、暂停均清除移动；文本输入与系统组合键不触发游戏快捷键。
- 时间自然推进，每日随机 240–360 秒，新世界随机上午开局；天气随机切换并持续 45–120 秒。保留人工时间及天气设置与平滑光照。
- 新增本地规则代理：发展家园、持续采集、维修巡查；A* 到达工作邻格后劳动结算，和鼠标操作共用采集、维修、建造规则。缺少材料、路线或位置被占用时显示等待状态。
- 托管优先处理雨天/雷雨/怪物/夜晚的回屋需求；转晴且安全后恢复计划。H 可单独回屋留守，手动操作随时接管。
- 真实结构判定：墙、门闭合围合，所在格有屋顶，入口可达。动态木门与室内屋顶隐藏；屋内收伞、恢复舒适度；户外雨和雷雨有不同暴露强度，影响行走与劳动速度。
- 屋内守卫会攻击近处怪物，并将身边建筑受到的拆房伤害从 18 降为 9。没有完整可达安全屋时明确提示，不会穿墙或传送进屋。
- 新存档字段保留可选验证，旧 v1 存档可读；保留玩家当前存档，测试使用独立浏览器上下文。
- 新增 4 项单元测试，合计 12 项通过；类型检查及生产构建通过。
- `scripts/check-survival.mjs` 在 1440×960、390×844、360×740 实测：键盘移动/停止/接管/失焦、墙体碰撞、E 核心维修扣 4 木材恢复 20 生命、自动采集及三处建造、雷雨中断、回屋关门、屋内防御、晴天恢复、H 回屋、失去屋顶、随机天气/跨日与托管存档恢复。三种尺寸无横向溢出、主要面板在视口内、画布非空、浏览器零错误。
- 证据：`artifacts/survival-qa.json` 和本地截图。截图查看工具不可用，未做人工图像内容验收；已做像素、布局及实际行为检查。
- 本次仍仅更新本地预览，未发布线上站点。
- 2026-09-07 完成最终回归：屋内收伞与雨滴屋顶阻挡、入口被堵时等待及解除后继续进屋均通过三种尺寸测试；隐藏屋顶不再拦截建筑点击。12 项单元测试、类型检查、生产构建再次通过。原有角色点击移动与劳动测试独立运行通过（并发图形测试曾超过 20 秒等待上限）；雷雨稳定性回归通过。

## 闪烁修复

- 2026-09-06：当前本地页面显示雷雨；复现发现闪电分支约每 2.33 秒将太阳光强度直接设为 7，与每 0.25 秒的基础光照更新相互覆盖，造成整屏亮度跳变。
- 移除雷雨的整屏强闪，保留雨滴和风；光照、天空、雾和火光改为逐帧平滑过渡，首帧直接使用存档对应的环境光。
- 修复失守存档读档后被设为未暂停的问题；核心生命值为零时停止模拟，暂停按钮不能重新激活已结束的世界，防止每帧反复提示。
- 桌面 1200×800、手机 390×844 各运行实际循环 720 帧（固定 1/30 秒步长）：雷雨晨间亮度修复前在 0.447 与 7 之间跳变，修复后稳定在约 0.447；雨滴可见、画布非空、采样期间零重载、零浏览器错误。
- 证据：`artifacts/flicker-before.json`、`artifacts/flicker-after.json`；回归脚本 `scripts/check-flicker.mjs`。
- 修复仅在本地预览生效，线上版本未更新。

## 当前交付

首个可玩版本已实现：建造、采集、修理、拆除、昼夜、三种天气、怪物拆房、自动守卫、失守与重开、本地存档。

2026-09-06 拟人化迭代（本地待确认）：
- 新增阿木：圆润五官、服饰、背包、眼眉表情、眨眼、打招呼和情境对白。
- 脚印模式可指定行走目的地；A* 避开建筑、资源、河流和核心；点击头像聚焦角色。
- 建造及修理挥锤、采集挥斧、下雨撑伞、夜间困倦、遇袭守家、击退后招手。
- 怪物新增迈步摆臂、拆房挥击、受击摇晃和不同步速。
- 旧版存档继续可读；新存档增加可选的角色位置并验证范围。
- 此阶段的鼠标资源与建筑效果为即时结算；最新键盘劳动及托管已改为到达并劳动后结算，鼠标保留原交互。

## 已验证

- 18 项规则单元测试通过，覆盖家具配方及库存事务、家具叠放与寻路、部件旋转、建造边界、屋顶支撑、占用、初始资源、损坏存档、角色位置验证、四方向寻路、庇护结构、随机天气、暴露恢复及代理存档兼容。
- TypeScript 检查通过；静态生产构建通过。
- npm 依赖安全审计：0 个已知漏洞（2026-09-06）。
- 浏览器实际点击建造墙和屋顶成功，资源扣除符合规则；采集增加 24 木材。
- 模拟 55 秒怪物移动攻击，部件从 86 减少到 68，验证真实拆房效果。
- 放置守卫塔后击退 3 只怪物，核心生命值保持 100。
- 手动保存后刷新，木材 250、建筑 65 个恢复一致。
- 1440×960、390×844、360×740 三种尺寸：画布非空、中心家园在视野内、河水在运动、按钮没有越界、无横向滚动和浏览器运行错误。
- 三种尺寸均实测雷雨和夜间切换；证据见 `artifacts/qa-results.json` 与本地截图。
- 当前工具不支持查看截图图像内容，未声称人工视觉审美验收；已执行布局、像素、可见性、运动与实际交互验证。
- 拟人化版在 1440×960 与 390×844 实测指定目的地行走、劳动挥锤动作、角色近景非空、雨伞、暂停冻结、新位置存档与旧版存档恢复；无浏览器错误。证据：`artifacts/character-qa.json`。

## 边界

- 单人、单地图，不含联网、第一人称操控和任意高度叠层；支持键盘移动、点击目的地寻路和网格家具布置。旋转不会改变网格占位；复杂布置仍需玩家自行保留屋内通路。
- 自动代理为本地规则驱动，不是联网大模型。发展计划为固定两塔一栅栏；维修仅针对仍存在的部件，不自动重建缺失屋顶或墙体。房屋庇护按网格围合及当前格屋顶判断，不是流体或气象模拟。
- 存档仅在当前浏览器来源下保留；清理浏览器数据会删除进度。
- 后台标签页停止游戏推进；没有离线怪物袭击。
- 世界资产为项目内程序化几何；较老设备可能需要缩小浏览器窗口以降低渲染开销。
- 木墙和木门共用建造格，屋顶依赖墙或门；初始木屋采用完整的预制屋顶分片。

## 发布

- 私有发布成功（2026-09-06），第 1 版。
- 访问地址： https://wildwood-homestead.staridea.chatgpt.site
- 已安排在 Codex 任务中打开线上页面。电脑锁屏导致无法确认前台页面显示；不影响已完成的发布。
- 发布源码：`727c7d935e31ef0258985eda04cbf3120fb81381`。
- 本文件为项目状态事实源；Obsidian 注册卡仅链接本目录，不复制项目正文。
