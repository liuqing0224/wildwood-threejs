# 项目规则

- 项目：林间筑梦（Wildwood），浏览器单人 3D 建造与防守游戏。
- `README.md` 维护启动与操作；`PROJECT_STATUS.md` 维护验证、状态和边界。
- 业务规则位于 `app/game/model.ts`，渲染与游戏循环位于 `app/game/engine.ts`，界面位于 `app/page.tsx`。
- 使用 Three.js 和现有 A* 库，不改成静态插画或假交互。
- 所有本地存档变更必须保持版本验证及损坏存档兜底。
- 修改后运行单元测试、类型检查与生产构建；涉及 3D 或布局时运行浏览器测试。
- 默认仅私有发布，不擅自扩大站点访问范围。
